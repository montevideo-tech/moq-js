{
	"name": "moq-js",
	"type": "module",
	"private": true,
	"workspaces": [
		"lib",
		"web",
		"simple-web"
	],
	"scripts": {
		"dev": "npm run build --workspace moq-player && cp ./lib/dist/iife/* ./simple-web/public/ && npm run dev --workspace simple-moq-web",
		"dev-web": "npm run dev --workspace web",
		"build": "npm run build --workspaces",
		"lint": "npm run lint --workspaces",
		"fmt": "npm run fmt --workspaces"
	}
}
